@model IEnumerable<DBW_Lab5.Models.Headquarter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.city_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.headquarter_address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.state)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.time_start)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.city_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.headquarter_address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.state)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.time_start)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.city_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.city_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.city_id })
        </td>
    </tr>
}

</table>
