@model IEnumerable<DBW_Lab5.Models.Store>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.time_start)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Headquarter.city_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StoreItem.store_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.time_start)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Headquarter.city_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StoreItem.store_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.store_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.store_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.store_id })
        </td>
    </tr>
}

</table>
